apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  selector:
    app: {{ .Values.name }}
  type: NodePort
  ports:
    - port: 8081
      protocol: TCP
      targetPort: 8000
      nodePort: 30000
